Config = {}

-- This script is solely meant for debugging, only enable it when needed. Otherwise put Config.Enabled to false.
Config.Enabled = true

--[[
    This script currently only supports ox_inventory and qb-inventory, but you can add additional inventories if you want.  
    Only do this if you have the knowledge to do so, as no support will be provided. If it works, submit a PR so I can include it for others.
    
    ox = 'ox_inventory'
    qb = 'qb-inventory'
]]
Config.Inventory = 'ox_inventory'

-- If the script checks for case sensitive
Config.CaseSensitive = false